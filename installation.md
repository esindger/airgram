# Installation

1. Build TDLib library according the [instruction](https://github.com/tdlib/td#building).
2. Install Airgram:

   ```bash
   # npm
   npm install airgram
   ```

   ```bash
   # yarn
   yarn add airgram
   ```

